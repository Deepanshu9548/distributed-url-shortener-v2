target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

.idea/
*.iml
.vscode/
.DS_Store

.env
load-tests/results/

.claude/settings.local.json

# local credentials — never commit (still needs `git rm --cached .claude/settings.json`)
.claude/settings.json
